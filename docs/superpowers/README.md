# Superpowers plans/specs — moved

This repo is public, so internal planning/spec docs (implementation plans, design
specs) no longer live here. They live in the internal (non-public) `GITHUB` workspace
root:

`GITHUB/docs/superpowers/plans/`
`GITHUB/docs/superpowers/specs/`

Edit/add new plans and specs there going forward — this folder is intentionally left
empty (aside from this note) so a future clone doesn't recreate them here by accident.
