# Brand docs — moved

This repo is public, so brand/content-strategy source-of-truth docs no
longer live here. They now live in the internal (non-public) `GITHUB`
workspace root, alongside the design-system docs:

`GITHUB/docs/brand/content-strategy.md`
`GITHUB/docs/brand/copywriting-voice-guide.md`
`GITHUB/docs/design-system/foundations.md`

**Why:** this repo's `main` branch is publicly visible on GitHub, and
those docs contain internal positioning/strategy content that shouldn't
be public. Edit the copies in the internal workspace going forward —
this folder is intentionally left empty (aside from this note) so a
future clone doesn't recreate the docs here by accident.
